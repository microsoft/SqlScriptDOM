@AGENTS.md

## Claude Code

- Treat `AGENTS.md` as the canonical shared instruction source for this repository.
- When a reusable workflow applies, consult the matching skill under `.agents/skills/`.
- Treat `TSql180` as the current vNext/latest parser target for shared agent workflows.