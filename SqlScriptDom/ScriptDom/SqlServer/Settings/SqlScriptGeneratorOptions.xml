<?xml version="1.0" encoding="utf-8" ?>
<!--
 <copyright file="ScriptGeneratorSettings.xml" company="Microsoft">
         Copyright (c) Microsoft Corporation.  All rights reserved.
 </copyright>
-->
<Settings>
  <SettingGroup name="Common">
    <Setting name="KeywordCasing" type="KeywordCasing" default="KeywordCasing.Uppercase">
      <Title>KeywordCasing_Title</Title>
      <Description>KeywordCasing_Description</Description>
      <Option value="KeywordCasing.Lowercase">KeywordCasing_LowerCase</Option>
      <Option value="KeywordCasing.Uppercase">KeywordCasing_UpperCase</Option>
      <Option value="KeywordCasing.PascalCase">KeywordCasing_PascalCase</Option>
      <Summary>Gets or sets the keyword casing option to use during script generation</Summary>
    </Setting>
    <Setting name="IdentifierCasing" type="IdentifierCasing" default="IdentifierCasing.Preserve">
      <Option value="IdentifierCasing.Preserve">IdentifierCasing_Preserve</Option>
      <Option value="IdentifierCasing.Uppercase">IdentifierCasing_UpperCase</Option>
      <Option value="IdentifierCasing.Lowercase">IdentifierCasing_LowerCase</Option>
      <Option value="IdentifierCasing.PascalCase">IdentifierCasing_PascalCase</Option>
      <Summary>Gets or sets the casing applied to object identifiers during script generation. This does not affect keywords, string literals, function names, or variables.</Summary>
    </Setting>
    <Setting name="IdentifierBracketing" type="IdentifierBracketing" default="IdentifierBracketing.Preserve">
      <Option value="IdentifierBracketing.Preserve">IdentifierBracketing_Preserve</Option>
      <Option value="IdentifierBracketing.IncludeBrackets">IdentifierBracketing_IncludeBrackets</Option>
      <Option value="IdentifierBracketing.ExcludeBrackets">IdentifierBracketing_ExcludeBrackets</Option>
      <Summary>Gets or sets how square brackets are applied to object identifiers during script generation. IncludeBrackets wraps all identifiers in square brackets; ExcludeBrackets removes brackets from identifiers that do not require them (identifiers conflicting with reserved words for the configured SqlVersion or containing special characters retain their brackets).</Summary>
    </Setting>
    <Setting name="BuiltInFunctionCasing" type="BuiltInFunctionCasing" default="BuiltInFunctionCasing.Preserve">
      <Option value="BuiltInFunctionCasing.Preserve">BuiltInFunctionCasing_Preserve</Option>
      <Option value="BuiltInFunctionCasing.Uppercase">BuiltInFunctionCasing_UpperCase</Option>
      <Option value="BuiltInFunctionCasing.Lowercase">BuiltInFunctionCasing_LowerCase</Option>
      <Option value="BuiltInFunctionCasing.PascalCase">BuiltInFunctionCasing_PascalCase</Option>
      <Summary>Gets or sets the casing applied to built-in / system function names (such as GETDATE, ISNULL, COALESCE, CAST, COUNT) during script generation. The default (Preserve) has no effect on the formatted output. When set to any other value it takes precedence over KeywordCasing for function names; under Preserve, keyword-backed built-ins such as CAST and CONVERT continue to follow KeywordCasing. Data type names, keywords, string literals, and variables are never affected. Casing is applied to every unqualified, non-delimited function name, which is safe because T-SQL resolves a one-part scalar function name as a built-in function and never as a user-defined one (a scalar user-defined function must be called with at least a two-part name). Schema-qualified (for example dbo.MyFunc) and delimited (for example [MyFunc]) function names, and method calls on a variable or column (for example @g.STArea()), are always preserved. Keyword-form niladic functions (such as CURRENT_TIMESTAMP, SYSTEM_USER) and built-in table-valued functions (such as STRING_SPLIT, OPENJSON) are not affected.</Summary>
    </Setting>
    <Setting name="SqlVersion" type="SqlVersion" default="SqlVersion.Sql90" browsable="false">
      <Summary>Gets or sets the Sql version to generate script for</Summary>
    </Setting>
    <Setting name="SqlEngineType" type="SqlEngineType" default="SqlEngineType.All" browsable="false">
      <Summary>Gets or sets the Sql engine type (All|Engine|Azure) to generate script for</Summary>
    </Setting>
    <Setting name="IndentationSize" type="int" default="4" min="0" browsable="false">
      <Summary>Gets or sets the number of spaces to use when indenting text. In Tabs mode each tab is treated as this many columns wide when computing alignment, so the viewer's tab size must be set to this value for the generated output to appear aligned.</Summary>
    </Setting>
    <Setting name="IndentationMode" type="IndentationMode" default="IndentationMode.Spaces">
      <Summary>Gets or sets whether indentation is written using spaces or tab characters. In Tabs mode the generated alignment only lines up when the viewer's tab width (for example the editor's tab size) equals IndentationSize. This setting is ignored (indentation uses spaces) when CommaPlacement is Leading.</Summary>
    </Setting>
    <Setting name="IncludeSemicolons" type="bool" default="false">
      <Title>IncludeSemicolons_Title</Title>
      <Description>IncludeSemiColons_Description</Description>
      <Summary>Gets or sets a boolean indicating if a semi colon should be included after each statement</Summary>
    </Setting>
    <Setting name="TerminateBlockStatements" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if a semicolon terminator is written after a BEGIN...END block and after the END CATCH of a BEGIN TRY...END TRY BEGIN CATCH...END CATCH block. When false (the default), these blocks are written without a terminator, preserving the previous script-generation behavior. When true, a semicolon is appended, so a terminator supplied in the source script survives a parse and generate round-trip. END TRY is never terminated because it is internal to the TRY...CATCH block.</Summary>
    </Setting>
    <Setting name="NewLineFormattedIndexDefinition" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if index definitions should have UNIQUE, INCLUDE and WHERE on their own line</Summary>
    </Setting>
    <Setting name="NewlineFormattedCheckConstraint" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if check constraints should have the CHECK part on it's own line</Summary>
    </Setting>
    <Setting name="SpaceBetweenDataTypeAndParameters" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if a space should be included between the data type and the parameters in a data type definition</Summary>
    </Setting>
    <Setting name="SpaceBetweenParametersInDataType" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if a space should be included between parameters in a data type</Summary>
    </Setting>
    <Setting name="NumNewlinesAfterStatement" type="int" default="1" min="0">
      <Summary>Gets or sets the number of newlines to include after each statement of a nested statement list, such as the body of a BEGIN...END block, stored procedure, trigger, function, IF or WHILE. The newlines are written between consecutive statements only, not after the last one, so the default of 1 puts each nested statement on its own line with no blank line between them. Statements owned directly by a batch are not affected by this setting; use NumNewlinesAfterBatchStatement for those.</Summary>
    </Setting>
    <Setting name="NumNewlinesAfterBatchStatement" type="int" default="2" min="0">
      <Summary>Gets or sets the number of newlines to include after each statement owned directly by a batch, that is, each top level statement of a script between GO separators. The newlines are written after every such statement, including the last one in the batch, except when the statement is a TSqlStatementSnippet (snippets are emitted as-is and do not receive additional trailing newlines from this option). The default of 2 leaves one blank line between consecutive top level statements and, together with the newline that always precedes GO, two blank lines before the GO that closes the batch. Statements nested inside another statement are not affected by this setting; use NumNewlinesAfterStatement for those.</Summary>
    </Setting>
    <Setting name="NumNewlinesAfterBatches" type="int" default="1" min="1">
      <Summary>Gets or sets the number of newlines to include after the GO separator that is written between two batches. GO is always preceded by a newline of its own so that it starts a new line, and the blank lines before it are controlled by NumNewlinesAfterBatchStatement; this setting only controls what follows GO, so the default of 1 starts the next batch on the following line with no blank line in between.</Summary>
    </Setting>
    <Setting name="PersistTrailingGo" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating whether the trailing GO batch separators are generated when the parsed script ended with them (see TSqlScript.TrailingGoCount). Defaults to false, so a trailing GO is not emitted and generation behavior is unchanged.</Summary>
    </Setting>
    <Setting name="PreserveComments" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if comments from the original script should be preserved in the generated output</Summary>
    </Setting>
    <Setting name="CommaPlacement" type="CommaPlacement" default="CommaPlacement.Trailing">
      <Summary>Gets or sets a value indicating whether commas in a multi-line comma-separated list are placed at the end of the line (trailing) or at the start of the next line (leading). When set to Leading, the IndentationMode setting is ignored and indentation always uses spaces.</Summary>
    </Setting>
    <Setting name="LeadingCommaSpaceCount" type="int" default="1" min="0" max="1">
      <Summary>Gets or sets the number of spaces written after a leading comma when CommaPlacement is Leading. Valid values are 0 and 1. The default is 1.</Summary>
    </Setting>
  </SettingGroup>
  <SettingGroup name="CREATE TABLE">
    <Setting name="AlignColumnDefinitionFields" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if column definition fields (column name, data type, constraints) should be aligned into alignment columns.</Summary>
    </Setting>
  </SettingGroup>
  <SettingGroup name="Clauses">
    <Setting name="NewLineBeforeFromClause" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if there should be a newline before the FROM clause in a SELECT statement</Summary>
    </Setting>
    <Setting name="NewLineBeforeWhereClause" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if there should be a newline before the WHERE clause in a SELECT statement</Summary>
    </Setting>
    <Setting name="NewLineBeforeGroupByClause" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if there should be a newline before the GROUP BY clause in a SELECT statement</Summary>
    </Setting>
    <Setting name="NewLineBeforeOrderByClause" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if there should be a newline before the ORDER BY clause in a SELECT statement</Summary>
    </Setting>
    <Setting name="NewLineBeforeHavingClause" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if there should be a newline before the HAVING clause in a SELECT statement</Summary>
    </Setting>
    <Setting name="NewLineBeforeWindowClause" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if there should be a newline before the WINDOW clause in a SELECT statement</Summary>
    </Setting>
    <Setting name="NewLineBeforeJoinClause" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if there should be a newline before the JOIN clause in a SELECT statement</Summary>
    </Setting>
    <Setting name="NewLineAfterJoinKeyword" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if there should be a newline after the JOIN keyword (before the table source). When false, the table source is kept on the same line as the JOIN keyword.</Summary>
    </Setting>
    <Setting name="NewLineBeforeOnClause" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if there should be a newline before the ON keyword of a JOIN. When false, the ON keyword is kept on the same line as the table source.</Summary>
    </Setting>
    <Setting name="NewLineBeforeOffsetClause" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if there should be a newline before the OFFSET clause</Summary>
    </Setting>
    <Setting name="NewLineBeforeOutputClause" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if there should be a newline before the OUTPUT clause</Summary>
    </Setting>
  </SettingGroup>
  <SettingGroup name="Alignment">
    <Setting name="ClauseBodyAlignment" type="ClauseBodyAlignment" default="ClauseBodyAlignment.Aligned">
      <Option value="ClauseBodyAlignment.Aligned">ClauseBodyAlignment_Aligned</Option>
      <Option value="ClauseBodyAlignment.Indented">ClauseBodyAlignment_Indented</Option>
      <Summary>Gets or sets how a clause body (the part after FROM, WHERE, GROUP BY, etc.) is laid out. Aligned keeps the body on the keyword's line, lining all clause bodies up under a shared column past the widest keyword. Indented puts the body on its own new line, one indent level (IndentationSize) past the keyword, so nesting grows one step per level instead of drifting right. When Indented, AlignClauseBodies is ignored for clause bodies.</Summary>
    </Setting>
    <Setting name="AlignClauseBodies" type="bool" default="true">
      <Summary>Gets or sets whether clause bodies are padded to line up under a shared column. Only applies when ClauseBodyAlignment is Aligned: true pads to the aligned column, false uses a single space after the keyword. When ClauseBodyAlignment is Indented, this setting is generally ignored for clause bodies; however, it also affects an INSERT statement's VALUES row constructors: when ClauseBodyAlignment is Indented and this is false, the row constructors move to their own indented line instead of being padded to line up under the column list.</Summary>
    </Setting>
  </SettingGroup>
  <SettingGroup name="SELECT">
    <Setting name="MultilineSelectElementsList" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if select elements should be listed as a multi-line list</Summary>
    </Setting>
    <Setting name="MultilineWherePredicatesList" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if WHERE predicates (expressions separated by AND, and OR) should be written on multiple lines</Summary>
    </Setting>
    <Setting name="MultilineGroupByElementsList" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if GROUP BY elements should be listed as a multi-line list</Summary>
    </Setting>
    <Setting name="MultilineHavingPredicatesList" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if HAVING predicates (expressions separated by AND or OR) should be written on multiple lines</Summary>
    </Setting>
    <Setting name="MultilineOrderByElementsList" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if ORDER BY elements should be listed as a multi-line list</Summary>
    </Setting>
    <Setting name="MultilinePartitionByElementsList" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if PARTITION BY elements in window specifications should be listed as a multi-line list</Summary>
    </Setting>
    <Setting name="MultilineInValuesList" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if the values in an IN (values) predicate should be listed as a multi-line list. When false, the IN list is written on a single line.</Summary>
    </Setting>
    <Setting name="ColumnAliasStyle" type="ColumnAliasStyle" default="ColumnAliasStyle.AsKeyword">
      <Option value="ColumnAliasStyle.AsKeyword">ColumnAliasStyle_AsKeyword</Option>
      <Option value="ColumnAliasStyle.EqualsSign">ColumnAliasStyle_EqualsSign</Option>
      <Option value="ColumnAliasStyle.Preserve">ColumnAliasStyle_Preserve</Option>
      <Summary>Gets or sets a value indicating how column aliases in SELECT projections are rendered (AS keyword, equals sign, or preserved from the original script)</Summary>
    </Setting>
  </SettingGroup>
  <SettingGroup name="CREATE/ALTER VIEW">
    <Setting name="IndentViewBody" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if the view statement body should be indented.</Summary>
    </Setting>
    <Setting name="MultilineViewColumnsList" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if view statement columns should be listed as a multi-line list</Summary>
    </Setting>
    <Setting name="AsKeywordOnOwnLine" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if the AS keyword should be written on its own line</Summary>
    </Setting>
  </SettingGroup>
  <SettingGroup name="UPDATE">
    <Setting name="IndentSetClause" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if the SET clause for an UPDATE statement should be indented</Summary>
    </Setting>
    <Setting name="AlignSetClauseItem" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if SET clause items in an UPDATE statement should be aligned</Summary>
    </Setting>
    <Setting name="MultilineSetClauseItems" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if SET clause items should be listed on multiple lines</Summary>
    </Setting>
  </SettingGroup>
  <SettingGroup name="INSERT">
    <Setting name="MultilineInsertTargetsList" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if the INSERT column targets list should be spread across multiple lines</Summary>
    </Setting>
    <Setting name="MultilineInsertSourcesList" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if the INSERT column sources list should be spread across multiple lines</Summary>
    </Setting>
  </SettingGroup>
  <SettingGroup name="CREATE/ALTER PROCEDURE, FUNCTION">
    <Setting name="MultilineProcedureParametersList" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if procedure and function parameters should be listed on separate lines. When false (the default), all parameters are written on a single line, preserving the previous script-generation behavior. When true, each parameter is placed on its own line, indented one level from the procedure or function name. CommaPlacement applies to the parameter list. AlignColumnDefinitionFields does not apply to procedure or function parameters.</Summary>
    </Setting>
  </SettingGroup>
  <SettingGroup name="Parenthesis">
    <Setting name="NewLineBeforeOpenParenthesisInMultilineList" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if a newline should be placed before an open parenthesis when writing a multi-line list in parenthesis</Summary>
    </Setting>
    <Setting name="NewLineBeforeCloseParenthesisInMultilineList" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if a newline should be placed before an close parenthesis when writing a multi-line list in parenthesis</Summary>
    </Setting>
  </SettingGroup>
  <SettingGroup name="Function calls">
    <Setting name="MultilineNestedFunctionCalls" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if parameters in a nested function call tree should be written on separate lines and indented one level per call. Isolated function calls remain on one line. When false (the default), all function calls retain the existing compact layout.</Summary>
    </Setting>
  </SettingGroup>
  <SettingGroup name="External Libraries">
    <Setting name="AllowExternalLibraryPaths" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if file paths can be used for external library content</Summary>
    </Setting>
  </SettingGroup>
  <SettingGroup name="External Languages">
    <Setting name="AllowExternalLanguagePaths" type="bool" default="true">
      <Summary>Gets or sets a boolean indicating if file paths can be used for external language content</Summary>
    </Setting>
  </SettingGroup>
  <SettingGroup name="WITH clause options">
    <Setting name="MultilineWithOptionsList" type="bool" default="false">
      <Summary>Gets or sets a boolean indicating if the options in a WITH (or OPTION) clause should be listed on separate lines. This applies to CREATE/ALTER INDEX and other index options, table hints (WITH), query hints (OPTION), and BACKUP/RESTORE options (WITH). It does not apply to the WITH keyword of common table expressions or XMLNAMESPACES, nor to the option lists of CREATE XML INDEX (non-selective) or CREATE SPATIAL INDEX, which have their own generators. When false (default), the options are written on a single line. CommaPlacement applies within the options list. For parenthesized clauses, NewLineBeforeOpenParenthesisInMultilineList and NewLineBeforeCloseParenthesisInMultilineList control parenthesis placement. When the WITH keyword begins the option list, the options are indented one level from the statement.</Summary>
    </Setting>
  </SettingGroup>
</Settings>
