CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();


GO
CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();


GO
CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();


GO
CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();


GO
CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();


GO
CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();


GO
CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

CREATE ENDPOINT e1 
    STATE = STOPPED
    AS TCP (
            LISTENER_IP = (1.2.3.4)
           )
    FOR TSQL();

